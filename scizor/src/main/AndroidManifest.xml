<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Lets the interface-tool overlays (grid, FPS, touches, frames) draw over the
         whole screen, including the Scizor menu and system bars. Requested at runtime
         when the user enables an overlay tool. -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />

    <application>
        <activity
            android:name="com.scizor.core.ScizorActivity"
            android:exported="false"
            android:configChanges="uiMode|orientation|screenSize|smallestScreenSize|screenLayout|keyboardHidden|density|fontScale"
            android:theme="@style/Theme.Scizor" />

        <service
            android:name="com.scizor.feature.notifications.ScizorNotificationListenerService"
            android:exported="false"
            android:label="Scizor Notification Logger"
            android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE">
            <intent-filter>
                <action android:name="android.service.notification.NotificationListenerService" />
            </intent-filter>
        </service>
    </application>

</manifest>
